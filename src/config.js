// src/config.js
export const DEFAULT_SHEET_ID = ""; // paste your Google Sheet ID here (the long ID in the URL)
export const DEFAULT_SHEETS = {
  teachers: "Teachers",
  subjects: "Subjects",
  classes: "Classes",
  allocation: "Allocation",
};
// How often to poll (ms) when Auto Refresh is ON
export const POLL_MS = 60000;
